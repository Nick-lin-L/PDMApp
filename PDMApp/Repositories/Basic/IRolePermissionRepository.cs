using PDMApp.Models;
using PDMApp.Parameters.Basic;
using PDMApp.Utils;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace PDMApp.Repositories.Basic
{
    public interface IRolePermissionRepository
    {
        /*
        Task<IDictionary<string, object>> GetRolePageInitialAsync();
        Task<PagedResult<pdm_roles>> GetRolesAsync(RolesParameter param);
        Task<pdm_roles> GetRoleByIdAsync(int roleId);
        Task AddRoleAsync(pdm_roles role);
        Task UpdateRoleAsync(pdm_roles role);
        Task AddOrUpdatePermissionsAsync(int roleId, List<PermissionRequest> permissions);
        Task AddOrUpdatePermissionDetailsAsync(int roleId, List<PermissionDetails> details);
        Task SaveChangesAsync();*/
    }

}
