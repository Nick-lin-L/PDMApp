using PDMApp.Models;
using PDMApp.Parameters.Basic;
using PDMApp.Utils;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace PDMApp.Service.Basic
{
    public interface IRolePermissionService
    {/*
        Task<IDictionary<string, object>> GetRolePageInitialAsync();
        Task<PagedResult<pdm_roles>> GetRolesAsync(RolesParameter param);
        Task<IDictionary<string, object>> UpsertRoleAsync(RolePermissionsParameter request);
        */
    }
}
